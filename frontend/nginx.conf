server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Quan trọng: mọi route không khớp file tĩnh đều trả về index.html
    # để React Router tự xử lý (tránh lỗi 404 khi F5 ở route con như /tasks)
    location / {
        try_files $uri $uri/ /index.html;
    }
}